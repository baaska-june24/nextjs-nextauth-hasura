table:
  name: sessions
  schema: user
object_relationships:
  - name: user
    using:
      manual_configuration:
        column_mapping:
          userId: id
        insertion_order: null
        remote_table:
          name: users
          schema: user
